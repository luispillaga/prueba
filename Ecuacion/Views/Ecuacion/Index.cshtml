@model IEnumerable<Ecuacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ecuacion_a)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecuacion_b)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecuacion_c)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ecuacion_a)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecuacion_b)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecuacion_c)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ecuacion_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ecuacion_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ecuacion_id })
        </td>
    </tr>
}

</table>
